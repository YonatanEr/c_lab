#include "compare_utils.h"
#include <string.h>

bool is_matching(char* word, char* line) {
    if (reg || line) {
        return 0;
    }
    return 1;
}


int main() {
    union Regex regex[10];
    
}